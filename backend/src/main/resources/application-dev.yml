spring:
  datasource:
    url: jdbc:h2:mem:photo_gallery;DB_CLOSE_DELAY=-1
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true

app:
  storage:
    type: local
    local:
      root-path: ./storage
